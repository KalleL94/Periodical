# Environment variables for Periodical production deployment
# Copy this file to .env and update the values

# SECRET_KEY - Used for JWT token signing (CRITICAL!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -base64 32
SECRET_KEY=CHANGE-ME-TO-RANDOM-SECRET-KEY-MINIMUM-32-CHARACTERS

# PRODUCTION - Set to 'true' in production (enables secure cookies, HTTPS-only)
PRODUCTION=true

# Database URL (optional, defaults to SQLite)
# DATABASE_URL=sqlite:///./app/database/schedule.db
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost/ica_schedule

# Logging level (optional)
# LOG_LEVEL=INFO

# CORS origins (optional, comma-separated)
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Timezone (optional, defaults to UTC)
TZ=Europe/Stockholm
